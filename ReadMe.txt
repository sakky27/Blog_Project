Running the code :

1. Installing the necessary library:

a. pip install django==2.2
b  Pip install django-taggit

2. After the needed library library is installed, opening the command prompt and running the application as to where the project folder is saved

	For Example saved in C Drive, then running steps as   1. c:\>cd blog_project
							      2. c:\blog_project>py manage.py runserver

3. After the server starts then opening the browser: for Admin : http://127.0.0.1:8000/admin/
     						and logging as Username: saksham  Password: saks1234 

4. For User opening the browser to view the blog : http://127.0.0.1:8000/

